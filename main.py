import gui
import overlay_solution
from os import listdir
from os.path import isfile, join

print("APP START")
start_gui = gui.StartGUI()
# after the main gui is done running, get its information and do everything


# onlyfiles = [f for f in listdir(mypath) if isfile(join(mypath, f))]

# only plays after the gui is closed
print('aa')
